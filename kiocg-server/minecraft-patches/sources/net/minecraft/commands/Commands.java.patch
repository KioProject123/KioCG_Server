--- a/net/minecraft/commands/Commands.java
+++ b/net/minecraft/commands/Commands.java
@@ -280,6 +_,9 @@
             StopCommand.register(this.dispatcher);
             TransferCommand.register(this.dispatcher);
             WhitelistCommand.register(this.dispatcher);
+            com.kiocg.command.TPSBarCommand.register(this.dispatcher); // KioCG - tps bar
+            com.kiocg.command.ChunkHotCommand.register(this.dispatcher); // KioCG - chunk hot
+            com.kiocg.command.TickerViewCommand.register(this.dispatcher); // KioCG - ticker view
         }
 
         if (selection.includeIntegrated) {
