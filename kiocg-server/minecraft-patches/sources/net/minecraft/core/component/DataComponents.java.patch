--- a/net/minecraft/core/component/DataComponents.java
+++ b/net/minecraft/core/component/DataComponents.java
@@ -147,7 +_,7 @@
     );
     public static final DataComponentType<Unit> INTANGIBLE_PROJECTILE = register("intangible_projectile", builder -> builder.persistent(Unit.CODEC));
     public static final DataComponentType<FoodProperties> FOOD = register(
-        "food", builder -> builder.persistent(FoodProperties.DIRECT_CODEC).networkSynchronized(FoodProperties.DIRECT_STREAM_CODEC).cacheEncoding()
+        "food", builder -> builder.persistent(FoodProperties.DIRECT_CODEC, FoodProperties.VANILLA_DIRECT_CODEC).networkSynchronized(FoodProperties.DIRECT_STREAM_CODEC).cacheEncoding() // KioCG - more food
     );
     public static final DataComponentType<Consumable> CONSUMABLE = register(
         "consumable", builder -> builder.persistent(Consumable.CODEC).networkSynchronized(Consumable.STREAM_CODEC).cacheEncoding()
@@ -231,13 +_,13 @@
         "debug_stick_state", builder -> builder.persistent(DebugStickState.CODEC).cacheEncoding()
     );
     public static final DataComponentType<CustomData> ENTITY_DATA = register(
-        "entity_data", builder -> builder.persistent(CustomData.CODEC_WITH_ID).networkSynchronized(CustomData.STREAM_CODEC)
+        "entity_data", builder -> builder.persistent(CustomData.CODEC_WITH_ID).networkSynchronized(io.papermc.paper.util.sanitizer.OversizedItemComponentSanitizer.CUSTOM_DATA) // KioCG - sanitize custom data
     );
     public static final DataComponentType<CustomData> BUCKET_ENTITY_DATA = register(
         "bucket_entity_data", builder -> builder.persistent(CustomData.CODEC).networkSynchronized(CustomData.STREAM_CODEC)
     );
     public static final DataComponentType<CustomData> BLOCK_ENTITY_DATA = register(
-        "block_entity_data", builder -> builder.persistent(CustomData.CODEC_WITH_ID).networkSynchronized(CustomData.STREAM_CODEC)
+        "block_entity_data", builder -> builder.persistent(CustomData.CODEC_WITH_ID).networkSynchronized(io.papermc.paper.util.sanitizer.OversizedItemComponentSanitizer.CUSTOM_DATA) // KioCG - sanitize custom data
     );
     public static final DataComponentType<InstrumentComponent> INSTRUMENT = register(
         "instrument", builder -> builder.persistent(InstrumentComponent.CODEC).networkSynchronized(InstrumentComponent.STREAM_CODEC).cacheEncoding()
@@ -393,4 +_,27 @@
     private static <T> DataComponentType<T> register(String name, UnaryOperator<DataComponentType.Builder<T>> builder) {
         return Registry.register(BuiltInRegistries.DATA_COMPONENT_TYPE, name, builder.apply(DataComponentType.builder()).build());
     }
+
+    // KioCG start - customize default components: 确保最后一个注册以免影响数据包
+    public static final DataComponentType<Unit> CANNOT_PLACE = register("cannot_place", builder -> builder.persistent(Unit.CODEC));
+    public static final DataComponentType<Unit> CANNOT_USE = register("cannot_use", builder -> builder.persistent(Unit.CODEC));
+    public static final DataComponentType<Unit> VALUABLE = register("valuable", builder -> builder.persistent(Unit.CODEC));
+    public static final DataComponentType<Unit> SLOT_LOCK = register("slot_lock", builder -> builder.persistent(Unit.CODEC));
+    public static final DataComponentType<Integer> CONTAINER_ROWS = register("container_rows", builder -> builder.persistent(ExtraCodecs.intRange(1, 6)));
+    public static final DataComponentType<ItemLore> EXTENDED_BEFORE_LORE = register("extended_before_lore", builder -> builder.persistent(ItemLore.CODEC));
+    public static final DataComponentType<ItemLore> EXTENDED_AFTER_LORE = register("extended_after_lore", builder -> builder.persistent(ItemLore.CODEC));
+    public static final DataComponentType<CustomData> STACKABLE_CUSTOM_DATA = register("stackable_custom_data", builder -> builder.persistent(CustomData.CODEC));
+    public static final DataComponentType<UseRemainder> ROT_INTO = register("rot_into", builder -> builder.persistent(UseRemainder.CODEC));
+
+    public static final java.util.Set<DataComponentType<?>> UNABLE_SEND_TO_CLIENT = java.util.Set.of(
+            CANNOT_PLACE,
+            CANNOT_USE,
+            VALUABLE,
+            SLOT_LOCK,
+            CONTAINER_ROWS,
+            EXTENDED_BEFORE_LORE,
+            EXTENDED_AFTER_LORE,
+            STACKABLE_CUSTOM_DATA,
+            ROT_INTO);
+    // KioCG end - customize default components: 确保最后一个注册以免影响数据包
 }
