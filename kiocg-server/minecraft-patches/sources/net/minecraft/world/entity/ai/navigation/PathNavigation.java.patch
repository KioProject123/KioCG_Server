--- a/net/minecraft/world/entity/ai/navigation/PathNavigation.java
+++ b/net/minecraft/world/entity/ai/navigation/PathNavigation.java
@@ -219,7 +_,20 @@
         return this.moveTo(this.createPath(x, y, z, accuracy), speed);
     }
 
+    // KioCG start - optimize entity behavior
+    private Entity prevEntity = null;
+    private int tickPause;
+    // KioCG end - optimize entity behavior
+
     public boolean moveTo(Entity entity, double speed) {
+        // KioCG start - optimize entity behavior
+        if (prevEntity == entity && entity.tickCount <= tickPause) {
+            return true;
+        }
+        prevEntity = entity;
+        tickPause = entity.tickCount + 8 + this.mob.numCollisions * 2;
+        // KioCG end - optimize entity behavior
+
         // Paper start - Perf: Optimise pathfinding
         if (this.pathfindFailures > 10 && this.path == null && net.minecraft.server.MinecraftServer.currentTick < this.lastFailure + 40) {
             return false;
