--- a/net/minecraft/world/entity/ai/goal/LandOnOwnersShoulderGoal.java
+++ b/net/minecraft/world/entity/ai/goal/LandOnOwnersShoulderGoal.java
@@ -11,13 +_,20 @@
         this.entity = entity;
     }
 
+    // KioCG start - optimize entity behavior
+    @Override
+    public boolean registerWhen() {
+        return this.entity.isTame() && !this.entity.isOrderedToSit();
+    }
+    // KioCG end - optimize entity behavior
+
     @Override
     public boolean canUse() {
         if (!(this.entity.getOwner() instanceof ServerPlayer serverPlayer)) {
             return false;
         } else {
             boolean flag = !serverPlayer.isSpectator() && !serverPlayer.getAbilities().flying && !serverPlayer.isInWater() && !serverPlayer.isInPowderSnow;
-            return !this.entity.isOrderedToSit() && flag && this.entity.canSitOnShoulder();
+            return flag && this.entity.canSitOnShoulder(); // KioCG - optimize entity behavior
         }
     }
 
