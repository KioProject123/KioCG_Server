--- a/net/minecraft/world/entity/item/ItemEntity.java
+++ b/net/minecraft/world/entity/item/ItemEntity.java
@@ -185,7 +_,10 @@
                 }
             }
 
-            if (!this.onGround() || this.getDeltaMovement().horizontalDistanceSqr() > 1.0E-5F || (this.tickCount + this.getId()) % 4 == 0) { // Paper - Diff on change; ActivationRange immunity
+            // KioCG start - item entity collision
+            boolean checkPush = true;
+            if (!this.onGround() || (checkPush = this.getDeltaMovement().horizontalDistanceSqr() > 1.0E-5F) || (this.tickCount + this.getId()) % 4 == 0) { // Paper - Diff on change; ActivationRange immunity
+            // KioCG end - item entity collision
                 this.move(MoverType.SELF, this.getDeltaMovement());
                 this.applyEffectsFromBlocks();
                 float f = 0.98F;
@@ -204,6 +_,8 @@
                         this.setDeltaMovement(deltaMovement1.multiply(1.0, -0.5, 1.0));
                     }
                 }
+
+                if (checkPush) this.pushEntities(); // KioCG - item entity collision
             }
 
             boolean flag = Mth.floor(this.xo) != Mth.floor(this.getX())
@@ -238,6 +_,37 @@
         }
     }
 
+    // KioCG start - item entity collision
+    protected void pushEntities() {
+        if (this.level().paperConfig().collisions.maxEntityCollisions <= 0) {
+            return;
+        }
+
+        this.numCollisions = Math.max(0, this.numCollisions - this.level().paperConfig().collisions.maxEntityCollisions);
+        if (this.numCollisions >= this.level().paperConfig().collisions.maxEntityCollisions) {
+            return;
+        }
+
+        java.util.List<ItemEntity> entities = this.level().getEntitiesOfClass(ItemEntity.class, this.getBoundingBox(), itemEntity -> itemEntity.isAlive() && itemEntity != this);
+        // Paper start - Cap entity collisions
+        for (Entity entity1 : entities) {
+            if (this.numCollisions >= this.level().paperConfig().collisions.maxEntityCollisions) {
+                break;
+            }
+
+            entity1.numCollisions++;
+            this.numCollisions++;
+            // Paper end - Cap entity collisions
+            entity1.push(this);
+        }
+    }
+
+    @Override
+    public boolean isCollidable(boolean ignoreClimbing) {
+        return this.isAlive();
+    }
+    // KioCG end - item entity collision
+
     @Override
     public BlockPos getBlockPosBelowThatAffectsMyMovement() {
         return this.getOnPos(0.999999F);
@@ -346,7 +_,7 @@
     public final boolean hurtServer(ServerLevel level, DamageSource damageSource, float amount) {
         if (this.isInvulnerableToBase(damageSource)) {
             return false;
-        } else if (!level.getGameRules().getBoolean(GameRules.RULE_MOBGRIEFING) && damageSource.getEntity() instanceof Mob) {
+        } else if (!level.getGameRules().getBoolean(GameRules.RULE_MOBGRIEFING1) && damageSource.getEntity() instanceof Mob) { // KioCG - better mob griefing setting
             return false;
         } else if (!this.getItem().canBeHurtBy(damageSource)) {
             return false;
@@ -370,6 +_,7 @@
 
     @Override
     public boolean ignoreExplosion(Explosion explosion) {
+        if (this.level().paperConfig().kiocgConfig.entity.itemEntityExplosionKnockback) return false; // KioCG - item entity explosion knockback
         return !explosion.shouldAffectBlocklikeEntities() || super.ignoreExplosion(explosion);
     }
 
@@ -485,7 +_,17 @@
             }
             // CraftBukkit end
             // Paper end - PlayerAttemptPickupItemEvent
-            if (this.pickupDelay == 0 && (this.target == null || this.target.equals(entity.getUUID())) && entity.getInventory().add(item)) {
+            // KioCG start - allow overstacked items: fix pickup animation
+            if (this.pickupDelay == 0 && (this.target == null || this.target.equals(entity.getUUID()))) {
+                if (!entity.getInventory().add(item) && flyAtPlayer) {
+                    org.bukkit.Location location = org.bukkit.craftbukkit.util.CraftLocation.toBukkit(this.position(), this.level().getWorld(), this.getYRot(), this.getXRot());
+                    org.bukkit.entity.Item fakeItem = this.level().getWorld().createEntity(location, org.bukkit.entity.Item.class);
+                    fakeItem.setItemStack(item.asBukkitMirror());
+                    fakeItem.getItemStack().setAmount(canHold);
+                    fakeItem.sendSpawn(new java.util.ArrayList<>(this.getBukkitEntity().getTrackedBy()), location);
+                    entity.take(((org.bukkit.craftbukkit.entity.CraftItem) fakeItem).getHandle(), count);
+                } else {
+            // KioCG end - allow overstacked items: fix pickup animation
                 if (flyAtPlayer) // Paper - PlayerPickupItemEvent
                 entity.take(this, count);
                 if (item.isEmpty()) {
@@ -496,6 +_,7 @@
                 entity.awardStat(Stats.ITEM_PICKED_UP.get(item1), count);
                 entity.onItemPickup(this);
             }
+            } // KioCG - allow overstacked items: fix pickup animation
         }
     }
 
