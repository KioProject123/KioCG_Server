--- a/net/minecraft/world/entity/AgeableMob.java
+++ b/net/minecraft/world/entity/AgeableMob.java
@@ -23,6 +_,12 @@
     protected int forcedAge = 0;
     protected int forcedAgeTimer;
     public boolean ageLocked; // CraftBukkit
+    // KioCG start - breeding animals
+    private int survivalDays;
+    private int feedingDays;
+    public int feedingToday;
+    public int produceToday;
+    // KioCG end - breeding animals
 
     protected AgeableMob(EntityType<? extends AgeableMob> entityType, Level level) {
         super(entityType, level);
@@ -69,6 +_,8 @@
     }
 
     public void ageUp(int amount, boolean forced) {
+        if (forced) { ageUp(); return; } // KioCG - breeding animals: 额外的保障
+
         if (this.ageLocked) return; // Paper - Honor ageLock
         int age = this.getAge();
         int previousAge = age; // Paper - Decompile fix: lvt reassignment lost
@@ -95,6 +_,19 @@
         this.ageUp(amount, false);
     }
 
+    // KioCG start - breeding animals
+    public void ageUp() {
+        if (!this.canFeed()) {
+            this.hurtServer(this.serverLevel(), this.damageSources().generic(), 2.0F);
+            return;
+        }
+
+        this.heal(2.0F, org.bukkit.event.entity.EntityRegainHealthEvent.RegainReason.EATING); // CraftBukkit
+        ageUp(240);
+        this.feedingFeed();
+    }
+    // KioCG end - breeding animals
+
     public void setAge(int age) {
         int age1 = this.getAge();
         this.age = age;
@@ -104,12 +_,42 @@
         }
     }
 
+    // KioCG start - breeding animals
+    public boolean canFeed() {
+        return this.feedingToday < (this.isHugeSimulate() ? 3 : 1);
+    }
+
+    public void feedingFeed() {
+        if (this.feedingToday++ == 0 && !this.isBaby()) {
+            if (++this.feedingDays >= this.level().paperConfig().kiocgConfig.entity.animal.feedingHugeStartDays) {
+                if (!this.isHugeSimulate() && this.random.nextFloat() < (float) this.level().paperConfig().kiocgConfig.entity.animal.feedingHugeChance) {
+                    this.setHugeSimulate();
+                }
+            }
+        }
+    }
+
+    public boolean canProduce() {
+        return this.produceToday < (this.isHugeSimulate() ? 3 : 1);
+    }
+
+    public void produce() {
+        ++produceToday;
+    }
+    // KioCG end - breeding animals
+
     @Override
     public void addAdditionalSaveData(CompoundTag compound) {
         super.addAdditionalSaveData(compound);
         compound.putInt("Age", this.getAge());
         compound.putInt("ForcedAge", this.forcedAge);
         compound.putBoolean("AgeLocked", this.ageLocked); // CraftBukkit
+        // KioCG start - breeding animals
+        compound.putInt("KioCG.SurvivalDays", this.survivalDays);
+        compound.putInt("KioCG.FeedingDays", this.feedingDays);
+        compound.putInt("KioCG.FeedingToday", this.feedingToday);
+        compound.putInt("KioCG.ProduceToday", this.produceToday);
+        // KioCG end - breeding animals
     }
 
     @Override
@@ -118,6 +_,12 @@
         this.setAge(compound.getIntOr("Age", 0));
         this.forcedAge = compound.getIntOr("ForcedAge", 0);
         this.ageLocked = compound.getBooleanOr("AgeLocked", false); // CraftBukkit
+        // KioCG start - breeding animals
+        this.survivalDays = compound.getIntOr("KioCG.SurvivalDays", 0);
+        this.feedingDays = compound.getIntOr("KioCG.FeedingDays", 0);
+        this.feedingToday = compound.getIntOr("KioCG.FeedingToday", 0);
+        this.produceToday = compound.getIntOr("KioCG.ProduceToday", 0);
+        // KioCG end - breeding animals
     }
 
     @Override
@@ -138,7 +_,7 @@
         } else {
             int age = this.getAge();
             if (age < 0) {
-                this.setAge(++age);
+                // this.setAge(++age); // KioCG - breeding animals: 阻止小动物自然生长
             } else if (age > 0) {
                 this.setAge(--age);
             }
@@ -160,10 +_,19 @@
         } else if (this.isAlive()) {
             int age = this.getAge();
             if (age < 0) {
-                this.setAge(++age);
+                // this.setAge(++age); // KioCG - breeding animals: 阻止小动物自然生长
             } else if (age > 0) {
                 this.setAge(--age);
             }
+
+            // KioCG start - breeding animals
+            final int days = this.level().getDays();
+            if (days != this.survivalDays) {
+                this.survivalDays = days;
+                this.feedingToday = 0;
+                this.produceToday = 0;
+            }
+            // KioCG end - breeding animals
         }
     }
 
@@ -179,7 +_,10 @@
     }
 
     @Override
-    public void setBaby(boolean baby) {
+    // KioCG start - entity baby api
+    public void setBaby1(boolean baby) {
+        if (baby) removeScaleAttributes();
+    // KioCG end - entity baby api
         this.setAge(baby ? -24000 : 0);
     }
 
