--- a/net/minecraft/world/entity/animal/Cat.java
+++ b/net/minecraft/world/entity/animal/Cat.java
@@ -97,19 +_,26 @@
         this.temptGoal = new Cat.CatTemptGoal(this, 0.6, stack -> stack.is(ItemTags.CAT_FOOD), true);
         this.goalSelector.addGoal(1, new FloatGoal(this));
         this.goalSelector.addGoal(1, new TamableAnimal.TamableAnimalPanicGoal(1.5));
-        this.goalSelector.addGoal(2, new SitWhenOrderedToGoal(this));
-        this.goalSelector.addGoal(3, new Cat.CatRelaxOnOwnerGoal(this));
-        this.goalSelector.addGoal(4, this.temptGoal);
-        this.goalSelector.addGoal(5, new CatLieOnBedGoal(this, 1.1, 8));
-        this.goalSelector.addGoal(6, new FollowOwnerGoal(this, 1.0, 10.0F, 5.0F));
-        this.goalSelector.addGoal(7, new CatSitOnBlockGoal(this, 0.8));
-        this.goalSelector.addGoal(8, new LeapAtTargetGoal(this, 0.3F));
-        this.goalSelector.addGoal(9, new OcelotAttackGoal(this));
-        this.goalSelector.addGoal(10, new BreedGoal(this, 0.8));
-        this.goalSelector.addGoal(11, new WaterAvoidingRandomStrollGoal(this, 0.8, 1.0000001E-5F));
+        // KioCG start - optimize entity behavior
+        this.goalRegister.addGoal(this.goalSelector, 2, new SitWhenOrderedToGoal(this));
+        this.goalRegister.addGoal(this.goalSelector, 3, new Cat.CatRelaxOnOwnerGoal(this));
+        this.goalRegister.addGoal(this.goalSelector, 4, this.temptGoal);
+        this.goalRegister.addGoal(this.goalSelector, 5, new CatLieOnBedGoal(this, 1.1, 8));
+        this.goalRegister.addGoal(this.goalSelector, 6, new FollowOwnerGoal(this, 1.0, 10.0F, 5.0F));
+        this.goalRegister.addGoal(this.goalSelector, 7, new CatSitOnBlockGoal(this, 0.8));
+        this.goalRegister.addGoal(this.goalSelector, 8, new LeapAtTargetGoal(this, 0.3F));
+        this.goalRegister.addGoal(this.goalSelector, 9, new OcelotAttackGoal(this));
+        this.goalRegister.addGoal(this.goalSelector, 10, new BreedGoal(this, 0.8), this::isTame);
+        this.goalSelector.addGoal(11, new WaterAvoidingRandomStrollGoal(this, 0.8, 1.0000001E-5F) {
+            @Override
+            public boolean canUse() {
+                return !((TamableAnimal) this.mob).isInSittingPose() && super.canUse();
+            }
+        });
         this.goalSelector.addGoal(12, new LookAtPlayerGoal(this, Player.class, 10.0F));
-        this.targetSelector.addGoal(1, new NonTameRandomTargetGoal<>(this, Rabbit.class, false, null));
-        this.targetSelector.addGoal(1, new NonTameRandomTargetGoal<>(this, Turtle.class, false, Turtle.BABY_ON_LAND_SELECTOR));
+        this.goalRegister.addGoal(this.targetSelector, 1, new NonTameRandomTargetGoal<>(this, Rabbit.class, false, null));
+        this.goalRegister.addGoal(this.targetSelector, 1, new NonTameRandomTargetGoal<>(this, Turtle.class, false, Turtle.BABY_ON_LAND_SELECTOR));
+        // KioCG end - optimize entity behavior
     }
 
     public Holder<CatVariant> getVariant() {
@@ -394,7 +_,19 @@
                         this.playEatingSound();
                     }
 
+                    return InteractionResult.SUCCESS_SERVER; // KioCG - dont kill my pet
+                    // KioCG start - dont kill my pet
+                } else if (itemInHand.is(net.minecraft.world.item.Items.POISONOUS_POTATO)) {
+                    this.playEatingSound();
+
+                    this.usePlayerItem(player, hand, itemInHand);
+                    this.addEffect(new net.minecraft.world.effect.MobEffectInstance(net.minecraft.world.effect.MobEffects.POISON, 900), org.bukkit.event.entity.EntityPotionEffectEvent.Cause.FOOD); // CraftBukkit
+                    if (player.isCreative() || !this.isInvulnerable()) {
+                        this.hurt(this.damageSources().playerAttack(player), Float.MAX_VALUE);
+                    }
+
                     return InteractionResult.SUCCESS;
+                    // KioCG end - dont kill my pet
                 }
 
                 InteractionResult interactionResult = super.mobInteract(player, hand);
@@ -497,11 +_,18 @@
             this.cat = cat;
         }
 
+        // KioCG start - optimize entity behavior
+        @Override
+        public boolean registerWhen() {
+            return this.cat.isTame() && !this.cat.isOrderedToSit();
+        }
+        // KioCG end - optimize entity behavior
+
         @Override
         public boolean canUse() {
-            if (!this.cat.isTame()) {
+            if (false && !this.cat.isTame()) { // KioCG - optimize entity behavior
                 return false;
-            } else if (this.cat.isOrderedToSit()) {
+            } else if (false && this.cat.isOrderedToSit()) { // KioCG - optimize entity behavior
                 return false;
             } else {
                 LivingEntity owner = this.cat.getOwner();
@@ -553,7 +_,7 @@
         public void start() {
             if (this.goalPos != null) {
                 this.cat.setInSittingPose(false);
-                this.cat.getNavigation().moveTo(this.goalPos.getX(), this.goalPos.getY(), this.goalPos.getZ(), 1.1F);
+                this.cat.getNavigation().moveToLightweight(this.goalPos.getX(), this.goalPos.getY(), this.goalPos.getZ(), 1.1F); // KioCG - optimize entity behavior
             }
         }
 
@@ -607,7 +_,8 @@
         public void tick() {
             if (this.ownerPlayer != null && this.goalPos != null) {
                 this.cat.setInSittingPose(false);
-                this.cat.getNavigation().moveTo(this.goalPos.getX(), this.goalPos.getY(), this.goalPos.getZ(), 1.1F);
+                if (this.cat.getNavigation().isDone()) // KioCG - optimize entity behavior
+                this.cat.getNavigation().moveToLightweight(this.goalPos.getX(), this.goalPos.getY(), this.goalPos.getZ(), 1.1F); // KioCG - optimize entity behavior
                 if (this.cat.distanceToSqr(this.ownerPlayer) < 2.5) {
                     this.onBedTicks++;
                     if (this.onBedTicks > this.adjustedTickDelay(16)) {
@@ -634,6 +_,13 @@
             this.cat = cat;
         }
 
+        // KioCG start - optimize entity behavior
+        @Override
+        public boolean registerWhen() {
+            return !this.cat.isTame();
+        }
+        // KioCG end - optimize entity behavior
+
         @Override
         public void tick() {
             super.tick();
@@ -651,7 +_,7 @@
 
         @Override
         public boolean canUse() {
-            return super.canUse() && !this.cat.isTame();
+            return super.canUse(); // KioCG - optimize entity behavior
         }
     }
 }
