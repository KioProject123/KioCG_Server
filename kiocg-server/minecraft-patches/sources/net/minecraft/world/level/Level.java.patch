--- a/net/minecraft/world/level/Level.java
+++ b/net/minecraft/world/level/Level.java
@@ -179,7 +_,7 @@
     }
     // Paper end - Use getChunkIfLoadedImmediately
     // Paper start - per world ticks per spawn
-    private int getTicksPerSpawn(SpawnCategory spawnCategory) {
+    public int getTicksPerSpawn(SpawnCategory spawnCategory) {
         final int perWorld = this.paperConfig().entities.spawning.ticksPerSpawn.getInt(org.bukkit.craftbukkit.util.CraftSpawnCategory.toNMS(spawnCategory));
         if (perWorld >= 0) {
             return perWorld;
@@ -1871,6 +_,16 @@
     public long getDayTime() {
         return this.levelData.getDayTime();
     }
+
+    // KioCG start - world data synchronization
+    public int getDays() {
+        return ((net.minecraft.world.level.storage.PrimaryLevelData) this.levelData).getDays();
+    }
+
+    public int getNights() {
+        return ((net.minecraft.world.level.storage.PrimaryLevelData) this.levelData).getNights();
+    }
+    // KioCG end - world data synchronization
 
     public boolean mayInteract(Entity entity, BlockPos pos) {
         return true;
