--- a/net/minecraft/world/item/alchemy/PotionBrewing.java
+++ b/net/minecraft/world/item/alchemy/PotionBrewing.java
@@ -15,25 +_,43 @@
 
 public class PotionBrewing {
     public static final int BREWING_TIME_SECONDS = 20;
-    public static final PotionBrewing EMPTY = new PotionBrewing(List.of(), List.of(), List.of());
+    public static final PotionBrewing EMPTY = new PotionBrewing(List.of(), List.of(), List.of(), List.of()); // KioCG - mix potion effect
     private final List<Ingredient> containers;
     private final List<PotionBrewing.Mix<Potion>> potionMixes;
+    private final List<PotionBrewing.Mix<Potion>> baseMixes; // KioCG - mix potion effect
     private final List<PotionBrewing.Mix<Item>> containerMixes;
     private final it.unimi.dsi.fastutil.objects.Object2ObjectLinkedOpenHashMap<org.bukkit.NamespacedKey, io.papermc.paper.potion.PaperPotionMix> customMixes = new it.unimi.dsi.fastutil.objects.Object2ObjectLinkedOpenHashMap<>(); // Paper - Custom Potion Mixes
 
-    PotionBrewing(List<Ingredient> containers, List<PotionBrewing.Mix<Potion>> potionMixes, List<PotionBrewing.Mix<Item>> containerMixes) {
+    PotionBrewing(List<Ingredient> containers, List<PotionBrewing.Mix<Potion>> potionMixes, List<PotionBrewing.Mix<Item>> containerMixes, List<PotionBrewing.Mix<Potion>> baseMixes) { // KioCG - mix potion effect
         this.containers = containers;
         this.potionMixes = potionMixes;
         this.containerMixes = containerMixes;
+        this.baseMixes = baseMixes; // KioCG - mix potion effect
     }
 
     public boolean isIngredient(ItemStack stack) {
         return this.isContainerIngredient(stack) || this.isPotionIngredient(stack) || this.isCustomIngredient(stack); // Paper - Custom Potion Mixes
     }
 
+    // KioCG start - vanilla recipe excludes model items
+    public boolean isPotion(ItemStack stack) {
+        if (this.isCustomInput(stack)) {
+            return true;
+        } else if (stack.is(Items.POTION) || stack.is(Items.SPLASH_POTION) || stack.is(Items.LINGERING_POTION) || stack.is(Items.GLASS_BOTTLE)) {
+            return net.minecraft.world.inventory.AbstractContainerMenu.allowModelItem(net.minecraft.world.inventory.MenuType.BREWING_STAND, stack);
+        }
+        return false;
+    }
+
+    public static boolean isFuel(ItemStack stack) {
+        return stack.is(net.minecraft.tags.ItemTags.BREWING_FUEL) && net.minecraft.world.inventory.AbstractContainerMenu.allowModelItem(net.minecraft.world.inventory.MenuType.BREWING_STAND, stack);
+    }
+    // KioCG end - vanilla recipe excludes model items
+
     private boolean isContainer(ItemStack stack) {
         for (Ingredient ingredient : this.containers) {
             if (ingredient.test(stack)) {
+                if (!net.minecraft.world.inventory.AbstractContainerMenu.allowModelItem(net.minecraft.world.inventory.MenuType.BREWING_STAND, stack)) return false; // KioCG - vanilla recipe excludes model items
                 return true;
             }
         }
@@ -44,6 +_,7 @@
     public boolean isContainerIngredient(ItemStack stack) {
         for (PotionBrewing.Mix<Item> mix : this.containerMixes) {
             if (mix.ingredient.test(stack)) {
+                if (!net.minecraft.world.inventory.AbstractContainerMenu.allowModelItem(net.minecraft.world.inventory.MenuType.BREWING_STAND, stack)) return false; // KioCG - vanilla recipe excludes model items
                 return true;
             }
         }
@@ -54,6 +_,7 @@
     public boolean isPotionIngredient(ItemStack stack) {
         for (PotionBrewing.Mix<Potion> mix : this.potionMixes) {
             if (mix.ingredient.test(stack)) {
+                if (!net.minecraft.world.inventory.AbstractContainerMenu.allowModelItem(net.minecraft.world.inventory.MenuType.BREWING_STAND, stack)) return false; // KioCG - vanilla recipe excludes model items
                 return true;
             }
         }
@@ -77,7 +_,7 @@
             return true;
         }
         // Paper end - Custom Potion Mixes
-        return this.isContainer(reagent) && (this.hasContainerMix(reagent, potionItem) || this.hasPotionMix(reagent, potionItem));
+        return this.isContainer(reagent) && (true || this.hasContainerMix(reagent, potionItem) || this.hasPotionMix(reagent, potionItem)); // KioCG - change container behavior: 自定义失败的酿造
     }
 
     public boolean hasContainerMix(ItemStack reagent, ItemStack potionItem) {
@@ -122,20 +_,53 @@
             } else {
                 for (PotionBrewing.Mix<Item> mix : this.containerMixes) {
                     if (potionItem.is(mix.from) && mix.ingredient.test(potion)) {
-                        return PotionContents.createItemStack(mix.to.value(), optional.get());
+                        if (!net.minecraft.world.inventory.AbstractContainerMenu.allowModelItem(net.minecraft.world.inventory.MenuType.BREWING_STAND, potionItem)) return potionItem; // KioCG - vanilla recipe excludes model items
+                        return setCustomEffects(PotionContents.createItemStack(mix.to.value(), optional.get()), potionItem.get(DataComponents.POTION_CONTENTS).customEffects()); // KioCG - mix potion effect
                     }
                 }
 
                 for (PotionBrewing.Mix<Potion> mixx : this.potionMixes) {
                     if (mixx.from.is(optional.get()) && mixx.ingredient.test(potion)) {
-                        return PotionContents.createItemStack(potionItem.getItem(), mixx.to);
-                    }
-                }
-
+                        if (!net.minecraft.world.inventory.AbstractContainerMenu.allowModelItem(net.minecraft.world.inventory.MenuType.BREWING_STAND, potionItem)) return potionItem; // KioCG - vanilla recipe excludes model items
+                        return setCustomEffects(PotionContents.createItemStack(potionItem.getItem(), mixx.to), potionItem.get(DataComponents.POTION_CONTENTS).customEffects()); // KioCG - mix potion effect
+                    }
+                }
+
+                // KioCG start - mix potion effect
+                if (!optional.get().value().getEffects().isEmpty()) {
+                    for (PotionBrewing.Mix<Potion> mixx : this.baseMixes) {
+                        if (mixx.ingredient.test(potion)) {
+                            if (!net.minecraft.world.inventory.AbstractContainerMenu.allowModelItem(net.minecraft.world.inventory.MenuType.BREWING_STAND, potionItem)) return potionItem; // KioCG - vanilla recipe excludes model items
+                            return setCustomEffects(PotionContents.createItemStack(potionItem.getItem(), mixx.to), potionItem.get(DataComponents.POTION_CONTENTS).getAllEffects());
+                        }
+                    }
+                }
+                // KioCG end - mix potion effect
+
+                // KioCG start - change container behavior: 自定义失败的酿造
+                potionItem = new ItemStack(potionItem.getItem());
+                potionItem.set(DataComponents.POTION_CONTENTS, PotionContents.EMPTY);
+                // KioCG end - change container behavior: 自定义失败的酿造
                 return potionItem;
             }
         }
     }
+
+    // KioCG start - mix potion effect
+    private ItemStack setCustomEffects(ItemStack result, Iterable<net.minecraft.world.effect.MobEffectInstance> effects) {
+        PotionContents resultContents = result.get(DataComponents.POTION_CONTENTS);
+        List<Holder<net.minecraft.world.effect.MobEffect>> resultEffects = resultContents.potion().get().value().getEffects().stream().map(net.minecraft.world.effect.MobEffectInstance::getEffect).toList();
+        java.util.Set<net.minecraft.world.effect.MobEffectInstance> treeSet = new java.util.TreeSet<>();
+        for (net.minecraft.world.effect.MobEffectInstance effect : effects) {
+            if (!resultEffects.contains(effect.getEffect())) {
+                treeSet.add(effect);
+            }
+        }
+
+        result.set(DataComponents.POTION_CONTENTS, new PotionContents(resultContents.potion(), resultContents.customColor(), List.copyOf(treeSet), resultContents.customName()));
+        return result;
+    }
+    // KioCG end - mix potion effect
 
     public static PotionBrewing bootstrap(FeatureFlagSet enabledFeatures) {
         PotionBrewing.Builder builder = new PotionBrewing.Builder(enabledFeatures);
@@ -170,7 +_,7 @@
         builder.addMix(Potions.LONG_LEAPING, Items.FERMENTED_SPIDER_EYE, Potions.LONG_SLOWNESS);
         builder.addMix(Potions.SLOWNESS, Items.REDSTONE, Potions.LONG_SLOWNESS);
         builder.addMix(Potions.SLOWNESS, Items.GLOWSTONE_DUST, Potions.STRONG_SLOWNESS);
-        builder.addMix(Potions.AWKWARD, Items.TURTLE_HELMET, Potions.TURTLE_MASTER);
+        builder.addMix(Potions.AWKWARD, Items.TURTLE_SCUTE, Potions.TURTLE_MASTER); // KioCG - 海龟壳改为海龟鳞甲
         builder.addMix(Potions.TURTLE_MASTER, Items.REDSTONE, Potions.LONG_TURTLE_MASTER);
         builder.addMix(Potions.TURTLE_MASTER, Items.GLOWSTONE_DUST, Potions.STRONG_TURTLE_MASTER);
         builder.addMix(Potions.SWIFTNESS, Items.FERMENTED_SPIDER_EYE, Potions.SLOWNESS);
@@ -250,6 +_,7 @@
     public static class Builder {
         private final List<Ingredient> containers = new ArrayList<>();
         private final List<PotionBrewing.Mix<Potion>> potionMixes = new ArrayList<>();
+        private final List<PotionBrewing.Mix<Potion>> baseMixes = new ArrayList<>(); // KioCG - mix potion effect
         private final List<PotionBrewing.Mix<Item>> containerMixes = new ArrayList<>();
         private final FeatureFlagSet enabledFeatures;
 
@@ -281,6 +_,7 @@
         public void addMix(Holder<Potion> input, Item reagent, Holder<Potion> result) {
             if (input.value().isEnabled(this.enabledFeatures) && reagent.isEnabled(this.enabledFeatures) && result.value().isEnabled(this.enabledFeatures)) {
                 this.potionMixes.add(new PotionBrewing.Mix<>(input, Ingredient.of(reagent), result));
+                if (input == Potions.AWKWARD) this.baseMixes.add(new PotionBrewing.Mix<>(input, Ingredient.of(reagent), result)); // KioCG - mix potion effect
             }
         }
 
@@ -292,7 +_,7 @@
         }
 
         public PotionBrewing build() {
-            return new PotionBrewing(List.copyOf(this.containers), List.copyOf(this.potionMixes), List.copyOf(this.containerMixes));
+            return new PotionBrewing(List.copyOf(this.containers), List.copyOf(this.potionMixes), List.copyOf(this.containerMixes), List.copyOf(this.baseMixes)); // KioCG - mix potion effect
         }
     }
 
