--- a/net/minecraft/world/item/ItemStack.java
+++ b/net/minecraft/world/item/ItemStack.java
@@ -101,17 +_,54 @@
         Component.translatable("item.op_warning.line3").withStyle(ChatFormatting.RED)
     );
     private static final Component UNBREAKABLE_TOOLTIP = Component.translatable("item.unbreakable").withStyle(ChatFormatting.BLUE);
+
+    public static final ThreadLocal<Boolean> REMOTE_HASH = ThreadLocal.withInitial(() -> false); // KioCG - customize default components
     public static final MapCodec<ItemStack> MAP_CODEC = MapCodec.recursive(
         "ItemStack",
         codec -> RecordCodecBuilder.mapCodec(
             instance -> instance.group(
                     Item.CODEC.fieldOf("id").forGetter(ItemStack::getItemHolder),
-                    ExtraCodecs.intRange(1, 99).fieldOf("count").orElse(1).forGetter(ItemStack::getCount),
-                    DataComponentPatch.CODEC.optionalFieldOf("components", DataComponentPatch.EMPTY).forGetter(stack -> stack.components.asPatch())
-                )
-                .apply(instance, ItemStack::new)
-        )
-    );
+                    ExtraCodecs.POSITIVE_INT.fieldOf("count").orElse(1).forGetter(ItemStack::getCount), // KioCG - allow overstacked items
+                    // KioCG start - customize default components
+                    DataComponentPatch.CODEC.optionalFieldOf("components", DataComponentPatch.EMPTY).forGetter(stack -> {
+                        if (REMOTE_HASH.get()) {
+                            return stack.getRemoteComponentsPatch();
+                        } else {
+                            return stack.components.asPatch();
+                        }
+                    })
+                    // KioCG end - customize default components
+                )
+                .apply(instance, ItemStack::new)
+        )
+    );
+    // KioCG start - customize default components
+    public static final MapCodec<ItemStack> REMOTE_MAP_CODEC = MapCodec.recursive(
+        "ItemStack",
+        codec -> RecordCodecBuilder.mapCodec(
+            instance -> instance.group(
+                    Item.CODEC.fieldOf("id").forGetter(ItemStack::getItemHolder),
+                    ExtraCodecs.POSITIVE_INT.fieldOf("count").orElse(1).forGetter(ItemStack::getCount), // KioCG - allow overstacked items
+                    DataComponentPatch.CODEC.optionalFieldOf("components", DataComponentPatch.EMPTY).forGetter(stack -> {
+                        // 流编解码以获得嵌套物品(收纳袋等)的内部物品的components.
+                        // 仍然有一些bug, 比如底层Item新增的组件将无法正常取得,
+                        // 例如, 食物的MAX_DAMAGE.
+                        // 这只会影响到消息链中的ShowItem, 因此修复它不是高优先级的.
+                        //TODO 需要在正确的地方设置ItemStack#REMOTE_HASH以使内部物品能够编码正确的RemoteComponentsPatch.
+                        io.netty.buffer.ByteBuf byteBuf = io.netty.buffer.ByteBufAllocator.DEFAULT.buffer();
+                        try {
+                            RegistryFriendlyByteBuf registryFriendlyByteBuf = new RegistryFriendlyByteBuf(byteBuf, net.minecraft.server.MinecraftServer.getServer().registryAccess());
+                            DataComponentPatch.STREAM_CODEC.encode(registryFriendlyByteBuf, stack.getRemoteComponentsPatch());
+                            return DataComponentPatch.STREAM_CODEC.decode(registryFriendlyByteBuf);
+                        } finally {
+                            byteBuf.release();
+                        }
+                    })
+                )
+                .apply(instance, ItemStack::new)
+        )
+    );
+    // KioCG end - customize default components
     public static final Codec<ItemStack> CODEC = Codec.lazyInitialized(MAP_CODEC::codec);
     public static final Codec<ItemStack> SINGLE_ITEM_CODEC = Codec.lazyInitialized(
         () -> RecordCodecBuilder.create(
@@ -189,6 +_,20 @@
                 } else {
                     Holder<Item> holder = Item.STREAM_CODEC.decode(buffer);
                     DataComponentPatch dataComponentPatch = codec.decode(buffer);
+                    // KioCG start - customize default components: 修复创造模式物品
+                    if (codec == DataComponentPatch.DELIMITED_STREAM_CODEC) {
+                        @Nullable Optional<? extends CustomData> optional = dataComponentPatch.get(DataComponents.CUSTOM_DATA);
+                        if (optional != null && optional.isPresent()) {
+                            Optional<DataComponentPatch> originalComponents = optional.flatMap(customData -> {
+                                RegistryOps<net.minecraft.nbt.Tag> registryOps = net.minecraft.server.MinecraftServer.getServer().registryAccess().createSerializationContext(net.minecraft.nbt.NbtOps.INSTANCE);
+                                return customData.getUnsafe().read("KioCG.OriginalComponents", DataComponentPatch.CODEC, registryOps);
+                            });
+                            if (originalComponents.isPresent()) {
+                                dataComponentPatch = originalComponents.get();
+                            }
+                        }
+                    }
+                    // KioCG end - customize default components: 修复创造模式物品
                     return new ItemStack(holder, varInt, dataComponentPatch);
                 }
             }
@@ -204,7 +_,7 @@
                     boolean prev = net.minecraft.network.chat.ComponentSerialization.DONT_RENDER_TRANSLATABLES.get();
                     try (final io.papermc.paper.util.SafeAutoClosable ignored = io.papermc.paper.util.sanitizer.ItemObfuscationSession.withContext(c -> c.itemStack(value))) { // pass the itemstack as context to the obfuscation session
                         net.minecraft.network.chat.ComponentSerialization.DONT_RENDER_TRANSLATABLES.set(true);
-                    codec.encode(buffer, value.components.asPatch());
+                    codec.encode(buffer, value.getRemoteComponentsPatch()); // KioCG - customize default components
                     } finally {
                         net.minecraft.network.chat.ComponentSerialization.DONT_RENDER_TRANSLATABLES.set(prev);
                     }
@@ -251,6 +_,54 @@
         return !this.isEmpty() ? this.components.asPatch() : DataComponentPatch.EMPTY;
     }
 
+    // KioCG start - customize default components
+    public DataComponentPatch getRemoteComponentsPatch() {
+        if (!this.isEmpty()) {
+            DataComponentPatch.Builder builder = DataComponentPatch.builder();
+            builder.copy(this.getItem().componentsPatchRemote());
+
+            boolean changed = false;
+            DataComponentPatch patch = this.components.asPatch();
+            for (java.util.Map.Entry<DataComponentType<?>, Optional<?>> entry : patch.entrySet()) {
+                if (!DataComponents.UNABLE_SEND_TO_CLIENT.contains(entry.getKey())) {
+                    entry.getValue().ifPresentOrElse(value -> builder.set((DataComponentType) entry.getKey(), value), () -> builder.remove(entry.getKey()));
+                } else {
+                    changed = true;
+                }
+            }
+
+            changed |= saveAdditional(builder);
+            if (changed && !patch.isEmpty()) {
+                net.minecraft.nbt.CompoundTag compoundTag = new net.minecraft.nbt.CompoundTag();
+                RegistryOps<net.minecraft.nbt.Tag> registryOps = net.minecraft.server.MinecraftServer.getServer().registryAccess().createSerializationContext(net.minecraft.nbt.NbtOps.INSTANCE);
+                compoundTag.store("KioCG.OriginalComponents", DataComponentPatch.CODEC, registryOps, patch);
+                builder.set(DataComponents.CUSTOM_DATA, CustomData.of(compoundTag));
+            }
+
+            return builder.build();
+        }
+        return DataComponentPatch.EMPTY;
+    }
+
+    private boolean saveAdditional(DataComponentPatch.Builder builder) {
+        boolean changed = false;
+
+        net.minecraft.world.item.component.ItemLore extendedBeforeLore = this.getOrDefault(DataComponents.EXTENDED_BEFORE_LORE, net.minecraft.world.item.component.ItemLore.EMPTY);
+        net.minecraft.world.item.component.ItemLore extendedAfterLore = this.getOrDefault(DataComponents.EXTENDED_AFTER_LORE, net.minecraft.world.item.component.ItemLore.EMPTY);
+        if (!extendedBeforeLore.lines().isEmpty() || !extendedAfterLore.lines().isEmpty()) {
+            net.minecraft.world.item.component.ItemLore lore = this.getOrDefault(DataComponents.LORE, net.minecraft.world.item.component.ItemLore.EMPTY);
+            List<Component> lores = new java.util.ArrayList<>();
+            lores.addAll(extendedBeforeLore.lines());
+            lores.addAll(lore.lines());
+            lores.addAll(extendedAfterLore.lines());
+            builder.set(DataComponents.LORE, new net.minecraft.world.item.component.ItemLore(lores));
+            changed = true;
+        }
+
+        return changed;
+    }
+    // KioCG end - customize default components
+
     public DataComponentMap immutableComponents() {
         return !this.isEmpty() ? this.components.toImmutableMap() : DataComponentMap.EMPTY;
     }
@@ -366,12 +_,28 @@
         return this.getItem().builtInRegistryHolder().tags();
     }
 
+    // KioCG start - prevent stacking containers
+    public boolean canFitInsideContainerItems() {
+        return this.getOrDefault(DataComponents.CONTAINER, ItemContainerContents.EMPTY).items.isEmpty() && this.getItem().canFitInsideContainerItems();
+    }
+    // KioCG end - prevent stacking containers
+
     public InteractionResult useOn(UseOnContext context) {
         Player player = context.getPlayer();
         BlockPos clickedPos = context.getClickedPos();
         if (player != null && !player.getAbilities().mayBuild && !this.canPlaceOnBlockInAdventureMode(new BlockInWorld(context.getLevel(), clickedPos, false))) {
             return InteractionResult.PASS;
         } else {
+            // KioCG start - customize default components
+            if (this.has(DataComponents.CANNOT_PLACE)) {
+                if (player != null) {
+                    player.containerMenu.forceHeldSlot(context.getHand());
+                    player.resyncUsingItem((ServerPlayer) player);
+                }
+                return InteractionResult.FAIL;
+            }
+            // KioCG end - customize default components
+
             Item item = this.getItem();
             // CraftBukkit start - handle all block place event logic here
             DataComponentPatch previousPatch = this.components.asPatch();
@@ -868,8 +_,20 @@
     }
 
     public static boolean isSameItemSameComponents(ItemStack stack, ItemStack other) {
-        return stack.is(other.getItem()) && (stack.isEmpty() && other.isEmpty() || Objects.equals(stack.components, other.components));
-    }
+        return stack.is(other.getItem()) && (stack.isEmpty() && other.isEmpty() || isSamePatchedComponents(stack.components, other.components)); // KioCG - customize default components
+    }
+
+    // KioCG start - customize default components
+    private static boolean isSamePatchedComponents(PatchedDataComponentMap stack, PatchedDataComponentMap other) {
+        return isSamePatchedComponents(stack, other, new java.util.HashSet<>());
+    }
+    private static boolean isSamePatchedComponents(PatchedDataComponentMap stack, PatchedDataComponentMap other, java.util.Set<DataComponentType<?>> excludeTypes) {
+        if (stack == other) return true;
+
+        excludeTypes.add(DataComponents.STACKABLE_CUSTOM_DATA);
+        return stack.asPatch().forget(excludeTypes).equals(other.asPatch().forget(excludeTypes));
+    }
+    // KioCG end - customize default components
 
     public static MapCodec<ItemStack> lenientOptionalFieldOf(String fieldName) {
         return CODEC.lenientOptionalFieldOf(fieldName)
@@ -1202,6 +_,12 @@
         EnchantmentHelper.updateEnchantments(this, mutable -> mutable.upgrade(enchantment, level));
     }
 
+    // KioCG start - allow unsafe enchant
+    public void setEnchant(Holder<Enchantment> enchantment, int level) {
+        EnchantmentHelper.updateEnchantments(this, enchantments -> enchantments.set(enchantment, level));
+    }
+    // KioCG end - allow unsafe enchant
+
     public boolean isEnchanted() {
         return !this.getOrDefault(DataComponents.ENCHANTMENTS, ItemEnchantments.EMPTY).isEmpty();
     }
@@ -1210,6 +_,14 @@
         return this.getOrDefault(DataComponents.ENCHANTMENTS, ItemEnchantments.EMPTY);
     }
 
+    // KioCG start - adjust enchantment
+    public int getEnchantmentLevel(net.minecraft.core.RegistryAccess registryManager, net.minecraft.resources.ResourceKey<Enchantment> enchantmentKey) {
+        return registryManager.lookupOrThrow(net.minecraft.core.registries.Registries.ENCHANTMENT).get(enchantmentKey)
+                              .map(reference -> getEnchantments().getLevel(reference))
+                              .orElse(0);
+    }
+    // KioCG end - adjust enchantment
+
     public boolean isFramed() {
         return this.entityRepresentation instanceof ItemFrame;
     }
@@ -1343,6 +_,15 @@
 
     public boolean isValidRepairItem(ItemStack item) {
         Repairable repairable = this.get(DataComponents.REPAIRABLE);
+        // KioCG start - more repair item
+        if (repairable == null) {
+            if (this.is(Items.SHEARS) && item.is(Items.IRON_INGOT)) return true;
+            if (this.is(Items.BOW) && item.is(Items.STRING)) return true;
+            if (this.is(Items.FISHING_ROD) && item.is(Items.STRING)) return true;
+            if (this.is(Items.FLINT_AND_STEEL) && item.is(Items.FLINT)) return true;
+            if (this.is(Items.BRUSH) && item.is(Items.FEATHER)) return true;
+        }
+        // KioCG end - more repair item
         return repairable != null && repairable.isValidRepairItem(item);
     }
 
