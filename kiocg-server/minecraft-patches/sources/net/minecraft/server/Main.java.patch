--- a/net/minecraft/server/Main.java
+++ b/net/minecraft/server/Main.java
@@ -315,6 +_,16 @@
                 return;
             }
 
+            // KioCG start - biome modification
+            try {
+                final com.kiocg.world.BiomeModification biomeModification = new com.kiocg.world.BiomeModification(worldStem.registries().compositeAccess());
+                biomeModification.addCustom();
+                biomeModification.applyModification();
+            } catch (Exception ex) {
+                ex.printStackTrace();
+            }
+            // KioCG end - biome modification
+
             /*
             RegistryAccess.Frozen frozen = worldStem.registries().compositeAccess();
             WorldData worldData = worldStem.worldData();
