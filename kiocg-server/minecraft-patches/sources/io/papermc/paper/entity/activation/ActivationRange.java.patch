--- a/io/papermc/paper/entity/activation/ActivationRange.java
+++ b/io/papermc/paper/entity/activation/ActivationRange.java
@@ -97,6 +_,11 @@
             || (entity.activationType == ActivationType.VILLAGER && config.villagerActivationRange <= 0)
             || (entity.activationType == ActivationType.WATER && config.waterActivationRange <= 0)
             || (entity.activationType == ActivationType.FLYING_MONSTER && config.flyingMonsterActivationRange <= 0)
+               // KioCG start - optimize entity activation
+               // SPIGOT-6644: Otherwise the target refresh tick will be missed
+               || entity instanceof ExperienceOrb
+               || entity instanceof net.minecraft.world.entity.monster.warden.Warden
+               // KioCG end - optimize entity activation
             || entity instanceof EyeOfEnder
             || entity instanceof Player
             || entity instanceof ThrowableProjectile
@@ -148,14 +_,16 @@
             }
 
             final int worldHeight = world.getHeight();
-            ActivationRange.maxBB = player.getBoundingBox().inflate(maxRange, worldHeight, maxRange);
-            ActivationType.MISC.boundingBox = player.getBoundingBox().inflate(miscActivationRange, worldHeight, miscActivationRange);
-            ActivationType.RAIDER.boundingBox = player.getBoundingBox().inflate(raiderActivationRange, worldHeight, raiderActivationRange);
-            ActivationType.ANIMAL.boundingBox = player.getBoundingBox().inflate(animalActivationRange, worldHeight, animalActivationRange);
-            ActivationType.MONSTER.boundingBox = player.getBoundingBox().inflate(monsterActivationRange, worldHeight, monsterActivationRange);
-            ActivationType.WATER.boundingBox = player.getBoundingBox().inflate(waterActivationRange, worldHeight, waterActivationRange);
-            ActivationType.FLYING_MONSTER.boundingBox = player.getBoundingBox().inflate(flyingActivationRange, worldHeight, flyingActivationRange);
-            ActivationType.VILLAGER.boundingBox = player.getBoundingBox().inflate(villagerActivationRange, worldHeight, villagerActivationRange);
+            // KioCG start - optimize entity activation
+            ActivationRange.maxBB = getActivationRange(player, maxRange, flyingActivationRange);
+            ActivationType.MISC.boundingBox = getActivationRange(player, miscActivationRange, 0);
+            ActivationType.RAIDER.boundingBox = getActivationRange(player, raiderActivationRange, 0);
+            ActivationType.ANIMAL.boundingBox = getActivationRange(player, animalActivationRange, 0);
+            ActivationType.MONSTER.boundingBox = getActivationRange(player, monsterActivationRange, 0);
+            ActivationType.WATER.boundingBox = getActivationRange(player, waterActivationRange, 0);
+            ActivationType.FLYING_MONSTER.boundingBox = getActivationRange(player, flyingActivationRange, flyingActivationRange);
+            ActivationType.VILLAGER.boundingBox = getActivationRange(player, villagerActivationRange, 0);
+            // KioCG end - optimize entity activation
 
             final List<Entity> entities = world.getEntities((Entity) null, ActivationRange.maxBB, e -> true);
             final boolean tickMarkers = world.paperConfig().entities.markers.tick;
@@ -169,6 +_,32 @@
         }
     }
 
+    // KioCG start - optimize entity activation
+    public static AABB getActivationRange(Player player, double xzRange, int extendSky) {
+        Level world = player.level();
+        if (!world.dimensionType().hasCeiling()) {
+            int oceanFloor = world.getHeight(net.minecraft.world.level.levelgen.Heightmap.Types.OCEAN_FLOOR, player.blockPosition());
+            if (player.getY() >= oceanFloor) {
+                return inflate(player.getBoundingBox(), xzRange, oceanFloor - 16, player.getY() + 16 + extendSky);
+            } else {
+                return inflate(player.getBoundingBox(), xzRange, player.getY() - 16, player.getY() + 16);
+            }
+        } else {
+            return player.getBoundingBox().inflate(xzRange, 48, xzRange);
+        }
+    }
+
+    public static AABB inflate(AABB aabb, double xz, double minY, double maxY) {
+        double d = aabb.minX - xz;
+        double d1 = minY;
+        double d2 = aabb.minZ - xz;
+        double d3 = aabb.maxX + xz;
+        double d4 = maxY;
+        double d5 = aabb.maxZ + xz;
+        return new AABB(d, d1, d2, d3, d4, d5);
+    }
+    // KioCG end - optimize entity activation
+
     /**
      * Tries to activate an entity.
      *
@@ -200,20 +_,21 @@
             return inactiveWakeUpImmunity;
         }
         if (entity.getRemainingFireTicks() > 0) {
-            return 2;
+            return entity.getRemainingFireTicks(); // KioCG - optimize entity activation
         }
         if (entity.activatedImmunityTick >= MinecraftServer.currentTick) {
-            return 1;
+            return (int) entity.activatedImmunityTick - MinecraftServer.currentTick; // KioCG - optimize entity activation
         }
         final long inactiveFor = MinecraftServer.currentTick - entity.activatedTick;
         if ((entity.activationType != ActivationType.WATER && entity.isInWater() && entity.isPushedByFluid())) {
-            return 100;
+            return 20; // KioCG - optimize entity activation
         }
+        if (entity instanceof ItemEntity) // KioCG - optimize entity activation: Check items only. https://github.com/PaperMC/Paper/pull/7644
         if (!entity.onGround() || entity.getDeltaMovement().horizontalDistanceSqr() > 9.999999747378752E-6D) {
-            return 100;
+            return 20; // KioCG - optimize entity activation
         }
         if (!(entity instanceof final AbstractArrow arrow)) {
-            if ((!entity.onGround() && !isEntityThatFlies(entity))) {
+            if ((!entity.onGround() && !isEntityThatFlies(entity)) && !entity.isInWater()) { // KioCG - optimize entity activation: !entity.isInWater()
                 return 10;
             }
         } else if (!arrow.isInGround()) {
@@ -230,8 +_,8 @@
             if (entity instanceof final Bee bee) {
                 final BlockPos movingTarget = bee.getMovingTarget();
                 if (bee.isAngry() ||
-                    (bee.getHivePos() != null && bee.getHivePos().equals(movingTarget)) ||
-                    (bee.getSavedFlowerPos() != null && bee.getSavedFlowerPos().equals(movingTarget))
+                    (false && bee.getHivePos() != null && bee.getHivePos().equals(movingTarget)) || // KioCG - breeding animals: 不再需要豁免蜜蜂
+                    (false && bee.getSavedFlowerPos() != null && bee.getSavedFlowerPos().equals(movingTarget)) // KioCG - breeding animals: 不再需要豁免蜜蜂
                 ) {
                     return 20;
                 }
@@ -257,10 +_,10 @@
                 return 1;
             }
             if (entity instanceof final Animal animal) {
-                if (animal.isBaby() || animal.isInLove()) {
+                if (animal.isInLove()) { // KioCG - breeding animals: 不再需要豁免baby
                     return 5;
                 }
-                if (entity instanceof final Sheep sheep && sheep.isSheared()) {
+                if (false && entity instanceof final Sheep sheep && sheep.isSheared()) { // KioCG - breeding animals: 不再需要豁免羊
                     return 1;
                 }
             }
@@ -275,9 +_,11 @@
             }
         }
         // SPIGOT-6644: Otherwise the target refresh tick will be missed
-        if (entity instanceof ExperienceOrb) {
-            return 20;
-        }
+        // KioCG start - optimize entity activation: 移动到initializeEntityActivationState()
+        // if (entity instanceof ExperienceOrb) {
+        //     return 20;
+        // }
+        // KioCG end - optimize entity activation: 移动到initializeEntityActivationState()
         return -1;
     }
 
@@ -289,12 +_,12 @@
      */
     public static boolean checkIfActive(final Entity entity) {
         // Never safe to skip fireworks or item gravity
-        if (entity instanceof FireworkRocketEntity || (entity instanceof ItemEntity && (entity.tickCount + entity.getId()) % 4 == 0)) { // Needed for item gravity, see ItemEntity tick
+        if ((entity instanceof ItemEntity && (entity.tickCount + entity.getId()) % 4 == 0)) { // Needed for item gravity, see ItemEntity tick // KioCG - optimize entity activation
             return true;
         }
         // special case always immunities
         // immunize brand-new entities, dead entities, and portal scenarios
-        if (entity.defaultActivationState || entity.tickCount < 20 * 10 || !entity.isAlive() || (entity.portalProcess != null && !entity.portalProcess.hasExpired()) || entity.portalCooldown > 0) {
+        if (entity.tickCount <= 20 || !entity.isAlive() || (entity.portalProcess != null && !entity.portalProcess.hasExpired()) || entity.portalCooldown > 0) { // KioCG - optimize entity activation: 降低出生豁免时间
             return true;
         }
         // immunize leashed entities
