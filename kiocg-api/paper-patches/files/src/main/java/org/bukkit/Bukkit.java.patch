--- a/src/main/java/org/bukkit/Bukkit.java
+++ b/src/main/java/org/bukkit/Bukkit.java
@@ -1132,6 +_,12 @@
         return server.addRecipe(recipe);
     }
 
+    // KioCG start - more api
+    public static boolean addRecipe(@NotNull List<Recipe> recipe) {
+        return server.addRecipe(recipe);
+    }
+    // KioCG end - more api
+
     /**
      * Adds a recipe to the crafting manager.
      *
@@ -1308,6 +_,13 @@
         return server.recipeIterator();
     }
 
+    // KioCG start - extended recipe iterator
+    @NotNull
+    public static <T extends Recipe> Iterator<T> recipeIterator(org.bukkit.inventory.RecipeType<T> recipeType) {
+        return server.recipeIterator(recipeType);
+    }
+    // KioCG end - extended recipe iterator
+
     /**
      * Clears the list of crafting recipes.
      */
@@ -1336,6 +_,12 @@
         return server.removeRecipe(key);
     }
 
+    // KioCG start - more api
+    public static boolean removeRecipe(@NotNull List<NamespacedKey> key) {
+        return server.removeRecipe(key);
+    }
+    // KioCG end - more api
+
     // Paper start - method to resend recipes
     /**
      * Remove a recipe from the server.
@@ -3000,6 +_,28 @@
         return server.isGlobalTickThread();
     }
     // Paper end - Folia region threading API
+
+    // KioCG start - more api
+    public static void registerItem(@NotNull NamespacedKey namespacedKey, @NotNull ItemStack itemStack) {
+        server.registerItem(namespacedKey, itemStack);
+    }
+
+    public static boolean unregisterItem(@NotNull NamespacedKey namespacedKey) {
+        return server.unregisterItem(namespacedKey);
+    }
+
+    public static @Nullable ItemStack getRegisterItem(@NotNull NamespacedKey namespacedKey) {
+        return server.getRegisterItem(namespacedKey);
+    }
+
+    public static @Nullable ItemStack getRegisterItem(@NotNull NamespacedKey namespacedKey, int amount) {
+        return server.getRegisterItem(namespacedKey, amount);
+    }
+
+    public static String formatStatistic(@NotNull Statistic statistic, int value) {
+        return server.formatStatistic(statistic, value);
+    }
+    // KioCG end - more api
 
     /**
      * @deprecated All methods on this class have been deprecated, see the individual methods for replacements.
